import React, { Component } from 'react'
import api from './api'

class TeacherResourcesPage  extends Component {
	constructor(props) {
		super(props) {
		teacherResourceFolder: []
		}
	}

render() {
	
		return (
			<section className="folder-list">
			{this.state.teacherResourceFolder.map((teacherResourceFolder, index) => (
					<section className="teacherResourceFolderInList" key={index}>
			)

	}

}
export default TeacherResourcesPage